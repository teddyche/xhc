fatal: [ANS_OLCSAP80]: FAILED! => {"msg": "The conditional check 'NombreTomcatProcess.results | default([]) | map(attribute='stdout', default='0') | map('int') | select('>', 0) | list | count > 0' failed. The error was: Unexpected keyword argument 'default'\n\nThe error appears to be in '/runner/project/playbook-start.yaml': line 36, column 11, but may\nbe elsewhere in the file depending on the exact syntax problem.\n\nThe offending line appears to be:\n\n\n        - name: Vérification si Tomcat est déjà démarré\n          ^ here\n"}
