TASK [Vérification si Tomcat est déjà démarré] *********************************
fatal: [ANS_OLCSAP80]: FAILED! => {"msg": "The conditional check 'NombreTomcatProcess.results | map(attribute='stdout') | map('int') | select('>', 0) | list | count > 0' failed. The error was: error while evaluating conditional (NombreTomcatProcess.results | map(attribute='stdout') | map('int') | select('>', 0) | list | count > 0): 'dict object' has no attribute 'stdout'. 'dict object' has no attribute 'stdout'\n\nThe error appears to be in '/runner/project/playbook-start.yaml': line 31, column 11, but may\nbe elsewhere in the file depending on the exact syntax problem.\n\nThe offending line appears to be:\n\n\n        - name: Vérification si Tomcat est déjà démarré\n          ^ here\n"}
