{% extends 'base.html.twig' %}

{% block title video.title %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">

            </div>
            <div class="col-md-4">
                <h1>{{ video.title }}</h1>
                <h2>{{ video.description }}</h2>
            </div>
        </div>
    </div>
{% endblock %}